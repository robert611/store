{% extends 'base.html.twig' %}

{% block title %}Log in!{% endblock %}

{% block body %}
    <div class="container mt-5">
        <div class="row">
            <div class="col-md-6">
                <div class="card no-border-radius">
                    <div class="card-body">
                        <form method="post">
                            {% if error %}
                                <div class="alert alert-danger">{{ error.messageKey|trans(error.messageData, 'security') }}</div>
                            {% endif %}

                            {% if app.user %}
                                <div class="mb-3">
                                    You are logged in as {{ app.user.username }}, <a href="{{ path('app_logout') }}">Logout</a>
                                </div>
                            {% endif %}

                            <h1 class="h3 mb-3 font-weight-normal">Logowanie</h1>

                            <div class="mb-3">
                                <label for="inputEmail">Email</label>
                                <input type="email" value="{{ last_username }}" name="email" id="inputEmail" class="form-control" required autofocus>
                            </div>

                            <div class="mb-3">
                                <label for="inputPassword">Password</label>
                                <input type="password" name="password" id="inputPassword" class="form-control" required>
                            </div>

                            <input type="hidden" name="_csrf_token"
                                value="{{ csrf_token('authenticate') }}"
                            >

                            <button class="btn btn-lg btn-primary no-border-radius" type="submit">
                                Zaloguj się
                            </button>
                        </form>
                    </div>
                </div>
            </div>

            <div class="col-md-6">
                <div class="card">
                    <div class="card-body">


                    </div>
                </div>
            </div>
        </div>

        <div class="row mt-3">
            <div class="col-md-6">
                <div class="card">
                    <div class="card-body p-4">
                        <h5 class="mb-0">Nie masz konta? <a href="{{ path('app_register') }}" class="register-link"><b>Zarejestruj się</b></a></h5>
                    </div>
                </div>
            </div>
        </div>

        <div class="row mt-3">
            <div class="col-md-4">
                <div class="card">
                    <div class="card-body login-entice-card p-4">
                        <i class="material-icons">people</i>

                        <h4 class="mb-1">Najniższe ceny!</h4>
                        <p>Dzieki dużej ofercie i wielu konkurujących firmach oferujemy niskie ceny</p>
                    </div>
                </div>
            </div>

            <div class="col-md-4">
                <div class="card">
                    <div class="card-body login-entice-card p-4">
                        <i class="material-icons">color_lens</i>

                        <h4 class="mb-1">Ogromny wybór!</h4>
                        <p>W naszej ofercie znajdziesz produkty wielu firm w każdej kategori</p>
                    </div>
                </div>
            </div>

            <div class="col-md-4">
                <div class="card">
                    <div class="card-body login-entice-card p-4">
                        <i class="material-icons">lock</i>

                        <h4 class="mb-1">Bezpieczne transakcje!</h4>
                        <p>W wypadku nieotrzymania zakupionego przedmiotu, zwracamy pieniądze</p>
                    </div>
                </div>
            </div>
        </div>
    </div>
{% endblock %}
